javavulnlab:
  build: .
  ports:
    - 8080:8080
  links: 
    - mysql

mysql:
  image: mysql
  environment:
    # Inject from host/.env; do not commit concrete secrets.
    - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
